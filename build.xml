<project name="selenium" default="all">
    <target name="all">
        <!--  output="${tmp}/ruby_out.txt" -->
        <property name="ruby.executable" value="ruby" />
        <exec executable="${ruby.executable}" failonerror="true">
            <arg line="selenium_example.rb"/>
        </exec>
    </target>
</project>
